# Bifrost MCP server (Phase 0) — dependencies for backend/mcp/ only.
#
# Kept separate from backend/requirements.txt so the main backend install
# doesn't pull the MCP SDK. This list is SELF-CONTAINED: backend.mcp imports
# only `mcp` + `httpx` (+ stdlib) and nothing from backend.app, so a *client*
# machine that only drives a remote backend can run the bridge with just these
# two — no PyPSA/FastAPI stack, no full backend install.
#
# Install:  <venv>/python -m pip install -r backend/mcp/requirements-mcp.txt

mcp>=1.28,<2
httpx>=0.27
