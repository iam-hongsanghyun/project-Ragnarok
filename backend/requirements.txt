fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pypsa==1.2.4  # 1.2.4 is released, so the Process component no longer needs the git pin this used to carry
pandas>=2.2
pyarrow>=17  # columnar Parquet engine for the server-side session store (time-series)
numpy>=2.1
scipy>=1.14
linopy>=0.5
highspy>=1.8
scikit-learn>=1.4  # k-means / HAC bus clustering (pypsa.clustering.spatial) — Forge "Reduce network"
statsmodels>=0.14  # ARIMA multi-year demand forecast (T1 "Forecast to future year")
prophet>=1.1       # Prophet multi-year demand forecast (T1); bundles a prebuilt Stan backend
openpyxl>=3.1
python-calamine>=0.2  # PyPSA's preferred excel engine for import_from_excel
xarray<2026
netCDF4>=1.6  # PyPSA-native binary export (.nc)
tables>=3.10  # PyPSA-native binary export (.h5)
# External-data importer subsystem (Data view) — runs server-side. The
# browser only POSTs a filter blob to /api/import/run; fetch + convert
# happen here so heavy datasets, CORS-blocked sources, and per-user API
# keys (BYOK) are all handled in Python.
httpx>=0.27          # upstream fetches (Overpass, GitHub, World Bank, …)
shapely>=2.0         # point-in-polygon clipping + region geometry

# MCP server (backend/mcp/server.py). Was undeclared, so a fresh install resolved
# to mcp 2.x, where `mcp.server.fastmcp` was renamed to `mcp.server.mcpserver` —
# the server then failed at import.
mcp>=1.2,<2
