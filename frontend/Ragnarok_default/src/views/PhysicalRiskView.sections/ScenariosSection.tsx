import React from 'react';
import { PortingPlaceholder } from './PortingPlaceholder';

export function ScenariosSection() {
  return (
    <PortingPlaceholder
      title="Scenarios"
      description="Climate + transition scenario picker (RCP pathway, target horizon, NGFS transition scenario) and the peril/run-config selection that seeds a run — ported from climaterisk's Scenarios tab."
    />
  );
}
