import React from 'react';
import { PortingPlaceholder } from './PortingPlaceholder';

export function ForecastSection() {
  return (
    <PortingPlaceholder
      title="Forecast"
      description="Multi-horizon impact forecast (present day through a future target year) so risk trajectories, not just point estimates, are visible — ported from climaterisk's Forecast tab."
    />
  );
}
