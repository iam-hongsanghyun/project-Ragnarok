import React from 'react';
import { PortingPlaceholder } from './PortingPlaceholder';

export function FinanceSection() {
  return (
    <PortingPlaceholder
      title="Finance"
      description="Financial-profile overlay translating physical + transition risk into credit-rating-relevant metrics (debt fraction, tenor, coverage thresholds) — ported from climaterisk's Finance tab."
    />
  );
}
