import React from 'react';
import { PortingPlaceholder } from './PortingPlaceholder';

export function MethodSection() {
  return (
    <PortingPlaceholder
      title="Method"
      description="Methodology and data-provenance page — which open-source engines and datasets back each number, spelled out honestly — ported from climaterisk's Method tab."
    />
  );
}
